# This file shows simple YAML key-value pairs.
# The word on the left is the key.
# The value on the right is the data for that key.

# This key stores the name of a server.
name: Webserver

# This key stores a port number.
port: 8080

# This key stores a true/false value.
aktiv: true
